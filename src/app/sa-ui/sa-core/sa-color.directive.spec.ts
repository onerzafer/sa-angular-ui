import { SaColorDirective } from './sa-color.directive';

describe('SaColorDirective', () => {
  it('should create an instance', () => {
    const directive = new SaColorDirective();
    expect(directive).toBeTruthy();
  });
});
